<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>com.commsen.em</groupId>
		<artifactId>em.demo</artifactId>
		<version>0.2.0-SNAPSHOT</version>
	</parent>
	
	<groupId>com.commsen.em.demo</groupId>
	<artifactId>em.demo.editor</artifactId>

	<properties>

		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>

		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

		<!-- 
			Prepares the artifacts needed to assemble an executable jar from.
			Those artifacts are placed in `<target>/export/<ArtifactId>` folder! 
			The `${_.eccentric.modularity.resolve.output}` property makes that path available to `maven-shade-plugin`!   
		 -->
		<_.eccentric.modularity.resolve />
		
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<!-- calculator implementations -->
				<groupId>com.commsen.em.demo</groupId>
				<artifactId>em.demo.calc-bom</artifactId>
				<version>${em.demo.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>


	<dependencies>

		<dependency>
			<groupId>com.commsen.em.demo</groupId>
			<artifactId>em.demo.markup</artifactId>
			<version>${em.demo.version}</version>
		</dependency>
		
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>3.0.0</version>
				<!-- 
					Need this as plugin dependency in order to use custom transformer
				 -->
				<dependencies>
					<dependency>
						<groupId>com.commsen.em</groupId>
						<artifactId>em-maven-extension</artifactId>
						<version>${em.version}</version>
					</dependency>
				</dependencies>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<createDependencyReducedPom>false</createDependencyReducedPom>
							<transformers>
								<!-- 
									Custom transformer for `maven-shade-plugin` to append additional artifacts   
								 -->
								<transformer
									implementation="com.commsen.em.maven.plugins.shade.AppendArtifactsTrnasformer">
									<location>${_.eccentric.modularity.resolve.output}</location>
								</transformer>
								<transformer
									implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
									<mainClass>com.commsen.em.demo.editor.Editor</mainClass>
								</transformer>
							</transformers>
							<artifactSet>
								<excludes>
									<exclude>com.commsen.em.demo:em.demo.index-calc</exclude>
								</excludes>
							</artifactSet>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>