<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.commsen.em.demo</groupId>
	<artifactId>rest</artifactId>
	<version>0.1.0-SNAPSHOT</version>

	<properties>
	
		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>

		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

	</properties>
	
	<profiles>
		<profile>
			<!-- 
				Use `mvn clean install` to build an executable jar to from this service.  
			 -->
			<id>default</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<properties>
				<!-- 
					This will build `<artifactId>.jar` executable jar in project's `target` folder!
				 -->
				<_.eccentric.modularity.executable />
			</properties>
		</profile>
		<profile>
			<!-- 
				Use `mvn clean install -P liferay` to get the artifacts you need to deploy to Liferay to run this service.  
			 -->
			<id>liferay</id>
			<properties>
				<!-- 
					The format for the target runtime is <host>:<port>:<name>:<version>.
					It will generate `.distros/<name>-<version>.jar` containing metadata about the runtime at <host>:<port> 
					which will be used by the resolver (together with dependent indexes) to determine which artifacts are needed.
					Those artifacts will be available in `<target>/export/<ArtifactId>` folder! 
					Maven plugin's can get that path from `${_.eccentric.modularity.resolve.output}` property! 
					
					KNOWN ISSUES / LIMITATIONS:
					 - the target runtime need to be a OSGi runtime where remote agent (https://mvnrepository.com/artifact/biz.aQute.bnd/biz.aQute.remote.agent) is installed
				 -->
				<_.eccentric.modularity.target.runtime>localhost:29998:liferay:7.0.0</_.eccentric.modularity.target.runtime>
			</properties>
		</profile>
	</profiles>

	<dependencies>

		<dependency>
			<groupId>com.commsen.em.demo</groupId>
			<artifactId>index-rest</artifactId>
			<version>0.1.0-SNAPSHOT</version>
			<type>index</type>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>com.commsen.em.demo</groupId>
			<artifactId>calc-api</artifactId>
			<version>0.1.0-SNAPSHOT</version>
		</dependency>

		<dependency>
			<groupId>javax.ws.rs</groupId>
			<artifactId>javax.ws.rs-api</artifactId>
			<version>2.0.1</version>
			<scope>provided</scope>
		</dependency>
		
		<dependency>
			<groupId>org.osgi</groupId>
			<artifactId>org.osgi.service.component.annotations</artifactId>
			<version>1.3.0</version>
			<scope>provided</scope>
		</dependency>
		
		<dependency>
			<groupId>biz.aQute.bnd</groupId>
			<artifactId>annotation</artifactId>
			<version>2.4.0</version>
			<scope>provided</scope>
		</dependency>

	</dependencies>
	
	<build>
		<extensions>
			<extension>
				<groupId>com.commsen.em</groupId>
				<artifactId>em-maven-extension</artifactId>
				<version>0.1.0-SNAPSHOT</version>
			</extension>
		</extensions>
	</build>
</project>